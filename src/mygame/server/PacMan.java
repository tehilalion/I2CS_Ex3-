package mygame.server;

public class PacMan {
  //  private int x;
    // private int y;

   /* public PacMan(int startX, int startY) {
        this.x = startX;
        this.y = startY;
    }

    public void move(int dir, GameBoard board) {
        int nx = x;
        int ny = y;

        if (dir == Game.UP) ny++;
        if (dir == Game.DOWN) ny--;
        if (dir == Game.LEFT) nx--;
        if (dir == Game.RIGHT) nx++;

        // wrap-around
        if (nx < 0) nx = board.getWidth() - 1;
        if (nx >= board.getWidth()) nx = 0;
        if (ny < 0) ny = board.getHeight() - 1;
        if (ny >= board.getHeight()) ny = 0;

        if (!board.isWall(nx, ny)) {
            x = nx;
            y = ny;
        }
    }

    public int getX() { return x; }
    public int getY() { return y; }

    */
}


